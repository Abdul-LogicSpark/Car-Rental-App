import { CustomFilterProps } from "@/types";
import React from "react";

const CustomFilter = ({title} : CustomFilterProps) => {
  return (
    <div>Custome filter</div>
  )
};

export default CustomFilter;
